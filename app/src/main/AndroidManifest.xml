<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ming.dayouxia.theaterapp" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity android:name=".TheaterDrawerMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".tabhostfragment.TheaterTestingActivity">


        </activity>

        <activity android:name=".unused.TheaterHomeTabsActivity">

        </activity>

        <activity android:name=".MovieDetailActivity"
            android:parentActivityName=".unused.TheaterHomeTabsActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".TheaterHomeListActivity"/>
        </activity>
    </application>

</manifest>
